export function Main({ children }) {
  return (
    <main>
      {/* Banner */}
      {/* Image Menu */}
      {/* Trendy collection */}
      {/* Deal of the week */}
      {children}
    </main>
  );
}
